name = "campanha-stats"
main = "src/index.js"
compatibility_date = "2025-01-01"

# Custom domain (configure no dashboard)
# routes = [{ pattern = "campanha-stats.datafixers.org", custom_domain = true }]

[[kv_namespaces]]
binding = "ANALYTICS"
id = "REPLACE_WITH_KV_NAMESPACE_ID"

# Secrets (defina via wrangler secret put):
#   wrangler secret put TG_TOKEN
#   wrangler secret put TG_CHAT
